version: 2

sources:
  - name: btc_transactions
    database: btc
    schema: btc_schema
    tables:
      - name: btc_transactions
        config:
          loaded_at_field: block_timestamp
          freshness:
            warn_after:
              count: 1
              period: hour
            error_after:
              count: 24
              period: hour
